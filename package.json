{
  "name": "natural-neighbors",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "cd code && npm install",
    "start": "cd code && node index.js"
  },
  "engines": {
    "node": "7.3.0"
  }
}
